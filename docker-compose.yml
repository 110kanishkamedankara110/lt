services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    restart: always
    ports:
      - "8001:80"
    volumes:
      - storage_volume_app1:/var/www/html/storage
      - uploads_volume_app1:/var/www/html/public/uploads

volumes:
  storage_volume_app2:
  uploads_volume_app2:
